# Editor / OS
.DS_Store
*.swp

# Local planning / tooling metadata
.getaibd/

# Build artifacts and signing key (never commit the private key)
extension.crx
extension.zip
extension.pem

# Node deps (MCP server)
node_modules/
mcp-server/node_modules/

# Build output
build/
